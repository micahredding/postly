<div class="podcast-index-wrapper">
<% @streams.each do |stream| %>
<div class="podcast-teaser">
  <h2 class="podcast-title"><a href="<%= StreamController.url_for(stream.stream_id) %>"><%= stream.title %></a></h2>
</div>
<% end %>
</div>
<br /><br /><br />